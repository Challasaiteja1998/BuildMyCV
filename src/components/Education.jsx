import { Box, Typography } from '@mui/material';

const Education = () => (
  <Box py={4}>
    <Typography variant="h5" gutterBottom>Education</Typography>
    <Typography variant="body1">AITS, Tirupati – B.Tech</Typography>
  </Box>
);

export default Education;
